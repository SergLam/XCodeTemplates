//___FILEHEADER___

import Foundation

enum UserDefaultsKeys: String {
    
    // MARK: - User data keys
    case userId = "userId"
    case accessToken = "accessToken"
    case deviceToken = "deviceToken"
    case isUserLoggedIn = "userWasLogged"
}
